service:
  name: serverless-api

plugins:
  - serverless-offline
  - serverless-webpack

custom:
  webpack:
    webpackConfig: ./webpack.config.js
    includeModules: true

provider:
  name: aws

functions:
  createUser:
    handler: src/contexts/users/infra/handlers/createUserHandle.handle
    events:
      - http:
          path: /users
          method: POST
          cors: true
  listUser:
    handler: src/contexts/users/infra/handlers/listUserHandle.handle
    events:
      - http:
          path: /users
          method: GET
          cors: true